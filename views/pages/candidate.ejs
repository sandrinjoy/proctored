<!DOCTYPE html>
<html>
  <head>
    <%- include ("../partials/header") %>

    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
  </head>
  <body oncopy="return false" oncut="return false" onpaste="return false">
    <%- include ("../partials/nav") %> <%- include ("../partials/loader") %> <%-
    include ("../partials/exam") %> <%- include ("../partials/chat") %>

    <script>
      var room = "<%- room %>";
      var ice = `<%- JSON.stringify(ice) %>`;
      var name = "<%- name %>";
      var id = "<%- id %>";
    </script>
    <script>
      function chaton() {
        document.getElementById("proctorchat").style.display = "block";
      }

      function chatoff() {
        document.getElementById("proctorchat").style.display = "none";
      }
    </script>

    <!-- The core Firebase JS SDK is always required and must be listed first -->
    <script src="https://www.gstatic.com/firebasejs/8.0.0/firebase-app.js"></script>

    <!-- TODO: Add SDKs for Firebase products that you want to use
     https://firebase.google.com/docs/web/setup#available-libraries -->

    <script src="https://www.gstatic.com/firebasejs/8.0.0/firebase-auth.js"></script>
    <script src="https://www.gstatic.com/firebasejs/8.0.0/firebase-firestore.js"></script>
    <script>
      // Your web app's Firebase configuration
      // For Firebase JS SDK v7.20.0 and later, measurementId is optional
      var firebaseConfig = {
        apiKey: "AIzaSyDoSTVmF7fGcqrcyuHKM52bH81iltDWj6w",
        authDomain: "hallofexams.firebaseapp.com",
        databaseURL: "https://hallofexams.firebaseio.com",
        projectId: "hallofexams",
        appId: "1:79320774776:web:8daa68fc748b367449d58d",
        measurementId: "G-1TNPJN728L",
      };
      // Initialize Firebase
      firebase.initializeApp(firebaseConfig);

      //make auth & firestore refs
      const auth = firebase.auth();
      const db = firebase.firestore();
    </script>
    <script src="/scripts/rtcClient.js"></script>
  </body>
</html>
